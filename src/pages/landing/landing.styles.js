import styled from "styled-components";

import { Box } from "@mui/material";

export const LandingPageSC = styled(Box)`
  display: flex;
  flex-direction: column;
  width: clamp(100%, 100%, 100%);
  min-height: 100vh;
`;